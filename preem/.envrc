# .envrc: direnv environment
#
# Delegates to the local flake and loads optional development secrets.

use flake .
dotenv_if_exists .env

mkdir -p .process_compose
export PC_LOG_FILE="$PWD/.process_compose/process-compose.log"
export PC_SOCKET_PATH="$PWD/.process_compose/process-compose.sock"
